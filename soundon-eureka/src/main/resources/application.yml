#指定eureka server的端口号为8761
server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
 #防止自己注册自己
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

